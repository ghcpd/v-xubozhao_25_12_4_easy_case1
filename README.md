## Plot Monthly Sales

Run the plotting script to generate charts from `monthl_sales.json`.

Install dependencies:

```powershell
python -m pip install -r requirements.txt
```

Generate charts:

```powershell
python .\scripts\plot_sales.py
```

Output images will be saved to `output/line_chart.png` and `output/bar_chart.png`.
